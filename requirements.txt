# Requirements for LID (Language Identification) pipeline
# Core scientific stack
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
scipy>=1.11
matplotlib>=3.8
joblib>=1.3

# Optional extras used only if installed (enable full experiments & dataset auto-download)
gensim>=4.3      # Word2Vec/Doc2Vec experiments
xgboost>=2.0,<3  # XGBoost (GPU optional via XGB_USE_GPU=1)
kagglehub        # Download dataset if LANGID_CSV not set
